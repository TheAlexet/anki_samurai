import 'package:flutter/material.dart';

class DojoScreen extends StatelessWidget {
  const DojoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dojo Page'),
    );
  }
}
